defaults:
  - _base_
  - override egopack_task: ego4d/oscc

optimizer:
  lr: 5e-5

# override model.depth
model:
  depth: 1

hydra:
  searchpath:
    - file://configs/experiments
    - file://configs/components